# Recurring Payments — Follow-Up Clarification Question

Your Q1 answer ("Automatch as in A but make sure a human reviews and agrees before you do it automatically in future") describes a two-phase behavior, but doesn't say whether the "automatic" phase ever actually stops needing review, or how broadly one approval extends.

## Clarification Question 1 — Scope of "Automatically in Future"
Once you've approved a match for a specific recurring payment (e.g. confirming that a particular transaction really was that month's payment for it), should later cycles of that *same* recurring payment then auto-apply without asking again?

A) Yes — approve it once for a given recurring payment, and every future cycle for that same payment auto-matches without review from then on (you can always go back and review/undo from history if something looks wrong)

B) Not fully automatic even after approval — every cycle still creates a reviewable match for you to confirm, but a previously-approved payment's matches are pre-checked/highlighted as "high confidence" so reviewing is faster (a rubber-stamp, not a skip)

C) Something in between — e.g. auto-apply only when the matched transaction's amount is very close to expected (per Q2's "loose sanity check"), otherwise always fall back to review, even for a previously-approved payment

D) Other (please describe after [Answer]: tag below)

[Answer]: C
