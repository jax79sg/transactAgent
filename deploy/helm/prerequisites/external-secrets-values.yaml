# Values for the official External Secrets Operator chart
# (repo: https://charts.external-secrets.io). No transactagent-specific customization
# needed -- defaults are fine, this is generic, reusable by any other app on the cluster.
